import { makeStyles } from '@mui/styles'

export const useStyles = makeStyles({
  root: {
    width: 230,
    height: 345,
  },
  card: {
    margin: 20,
  },
  container: {
    maxWidth: 'none',
    textAlign: 'center',
  },
  typography: {
    height: 40,
  },
})
